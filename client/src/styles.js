import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    grow: {
        flexGrow: 1,
    },
    heading: {
        color: 'rgba(0,183,255, 1)',
    },
    mainContainer: {
        minHeight: '100vh',
        margin: '30px auto',
    },
}));
