import React from 'react';
import { Typography } from '@material-ui/core';
import useStyles from './styles';
const Games = () => {
    const classes = useStyles();
    return <Typography>List Game</Typography>;
};

export default Games;
